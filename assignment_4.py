names_list = ["bob","jimmy","max b", "bernie", "jordan", "future hendrix", "wiuegbwibgiwuebowue"]

longest_name = -1
for name in names_list:
    if len(name) > (longest_name):
        longest_name = len(name)
        temp = name
print (temp)